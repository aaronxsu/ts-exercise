{
  "name": "section-10-desgin-patterns-more",
  "version": "1.0.0",
  "description": "- `npm install -g typescript ts-node` to install typescript compiler - `tsc --init` generates a `tsconfig.json` - modify below:",
  "main": "index.js",
  "scripts": {
    "start:build": "tsc -w",
    "start:run": "nodemon build/index.js",
    "start": "concurrently npm:start:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.2",
    "nodemon": "^1.19.1"
  }
}
